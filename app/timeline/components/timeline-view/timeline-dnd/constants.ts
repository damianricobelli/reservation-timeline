/** Timeline date key format used across drag/resize internals. */
export const DATE_KEY_FORMAT = "YYYY-MM-DD";

/** Data discriminator for draggable reservation blocks. */
export const RESERVATION_DRAG_KIND = "timeline-reservation";

/** Data discriminator for droppable timeline rows. */
export const ROW_DROP_KIND = "timeline-row";
